import React from 'react';
import { useGlobalStyles } from '@styles/global';

export const App = () => {
  useGlobalStyles();

  return (
    <div>
      <h1>Level Up - Web UI</h1>
    </div>
  );
};
